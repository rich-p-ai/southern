# Example credentials file — copy to .env and fill in your own values.
# Never commit the real .env (it is gitignored).

# OpenShift username (e.g. an active-directory account)
export OC_USER='YOUR-AD-USERNAME'

# OpenShift password — short-lived; rotate weekly per OpenShift policy
export OC_PASSWORD='YOUR-PASSWORD'
